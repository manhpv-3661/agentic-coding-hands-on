/**
 * Fonts required by the Figma design for the Login screen.
 *
 * Re-exported from the canonical `app/fonts.ts` module — Montserrat is now
 * wired site-wide via the root layout, so this file exists only to keep
 * `page.tsx` / `awards/page.tsx` / `kudos/page.tsx` importing from their
 * current path unchanged.
 */
export { montserrat, montserratAlternates } from "@/app/fonts";
